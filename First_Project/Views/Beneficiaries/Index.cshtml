@model IEnumerable<First_Project.Models.Beneficiary>

@{
    ViewData["Title"] = "Index";
    Layout = "_AdminLayout";
}

<h1>Index</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table-responsive  pt-3">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Userid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subscriptionid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Relationship)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Userid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subscriptionid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Relationship)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.Beneficiaryid">Edit</a> |*@
                <a asp-action="Details" asp-route-id="@item.Beneficiaryid">Details</a>@* |
                <a asp-action="Delete" asp-route-id="@item.Beneficiaryid">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
